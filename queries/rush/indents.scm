[
  (call_expr)
  (assign_expr)
  (block)
] @indent
(call_expr
  args: (_) @indent)
(function_definition
  params: (_) @indent)

(block "}" @indent_end)

[
  ")"
  "}"
] @branch

(line_comment) @ignore
